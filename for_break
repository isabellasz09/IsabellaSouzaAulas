presentes = ['John Snow','Jesse Pickman','Aria Stark','Tyrion Lannister']
chamada= 'Aria Stark'
for pessoa in presentes:
    if pessoa == chamada:
        print('{} está presente.'.format(chamada))
        break
    else:
        print('Só um print para mostrar que o for passou por esse pessoa: ' +str(pessoa))


presentes = ['John Snow','Jesse Pickman','Aria Stark','Tyrion Lannister']
chamada= 'Aria Stark'
for pessoa in presentes:
    if pessoa == chamada:
        print('{} está presente.'.format(chamada))
        break
    else:
        print('Só um print para mostrar que o for passou por esse pessoa: ' +str(pessoa))
        continue
